put logsThreeDotsRepeatedSeeds.zip and trainedModelsThreeDotsRepeatedSeeds.zip here and unzip them


These models are fine individually, but the ordering of random batches of data is the same between runs (typo in seed when randomizing order of training data), so just to be safe we re-ran these models for batch statistics calculations.
